# 페리에 현안

> 진행 중·미해결 항목만. 끝난 일은 여기 남기지 않고 지웁니다.
> 마지막 갱신: 2026-09-01

## 지금 물고 있는 전제 (틀리면 숫자가 다 흔들림)

| 전제 | 현재 값 | 상태 |
|---|---|---|
| 관세율 | **0%** (한·EU FTA 협정세율) | ⚠️ 미확정 |
| 기준 환율 | EUR 1,850 / USD 1,600 (2026-06-12 매매기준율 ×1.03·50원 올림) | 3개월 경과 |
| 기준 판매량 | 2026-06 실적 (`volJun`) | 3개월 경과 |
| 운임·통관 단가 | 모델 가정값 (실측 아님) | 미검증 |

---

## 1. 관세 분류 확정 — 최우선

마진율 전체가 관세 0% 위에 서 있습니다. 확정 전까지 **모든 마진 수치는 과대 가능성**이 있습니다.

- [ ] 향첨가 8종(HS 2202.10) 협정세율 적용 여부 관세사 confirm
  - 플레인 유리 3종(HS 2201.10)은 상대적으로 명확, 향첨가 8종이 쟁점
- [ ] confirm 결과를 `lib/model.ts`의 `BASE.tariff`에 반영
- [ ] 0%가 아닐 경우 마진 경보 SKU 재산정 (현재 경보 4종 → 늘어날 수 있음)

## 2. 데이터가 아직 모델 가정 — 실측으로 교체

현재 `lib/model.ts`의 SKU 마스터는 Google Sheet `products` 탭 실측 + recon 집계
(₩130,244,500 · 마진 13.1% · 경보 4종)에 **맞춰 역산한 모델**입니다. 운임·통관은 순수 가정값.

- [ ] 운임 단가 실측 확보 (현재 병당 USD 고정값, 슬라이더로만 조정 가능)
- [ ] 통관·포워딩 요율 실측 확보 (현재 CIF의 2.5% 가정)
- [ ] Google Sheets 라이브 연동 — `products`·`prices`·`sales` 탭 → ISR (PRD §5)
- [ ] Python 원가모델 CSV 연동 — `model/output/*.csv` 빌드타임 주입
- [ ] 기준 시점 갱신: 환율 2026-06-12 → 최신, 판매량 2026-06 → 최신월
- [ ] `MONTHS` 배열이 2026-01~06에서 멈춰 있음 (월별 추이 그래프가 6월까지만)

## 3. 참조 문서가 레포에 없음

README와 코드 주석이 아래 파일들을 가리키는데, **두 레포 어디에도 없습니다.**
로컬 어딘가에만 있는 상태로 보입니다. 기기를 옮겨 다니면 못 봅니다.

- [ ] `dashboard-prd.md` — 기획 원본
- [ ] `docs/calc-logic.md` — 원가 계산 로직 상세
- [ ] `model/` — Python 원가 모델
- [ ] 위 3개를 레포에 커밋할지, 별도 레포로 뺄지 결정 (민감정보 포함 여부 확인 필요)

## 4. 배포 — 아직 로컬에서만 뜸

임원용 대시보드인데 링크로 공유가 안 됩니다. `npm run dev` 로만 볼 수 있는 상태.

- [ ] 배포처 결정 (Vercel 등) — 사내 접근 정책 확인 필요
- [ ] `ECOS_API_KEY` 환경변수 설정 — 없으면 환율 배너가 503으로 죽음 (`app/api/fx/route.ts`)
- [ ] 접근 통제 — 공급가·마진은 대외비. 공개 URL로 두면 안 됨
- [ ] 업로드 판매 데이터가 브라우저 `localStorage` 에만 저장됨 → 기기 바뀌면 사라지고 공유 안 됨. 서버 저장 필요 여부 판단

## 5. 미구현 화면 (PRD 대비)

- [ ] 레이아웃 토글 — `경영진` / `분석가` / `경보우선` (variant CSS는 `globals.css`에 이미 있음, UI만 없음)
- [ ] PRD View 2 — 환율 추이 확장
- [ ] PRD View 3 — 시뮬레이터 확장
- [ ] AI 인사이트 패널

## 6. 월간 리포트 (`JayPark91/perrier`)

- [ ] **2026-08 리포트 미발행** — 현재 `2026-06`, `2026-07` 까지만 있음
  (7월분이 8/7 발행이었으니 8월분도 9월 초 주기. 오늘 9/1)
- [ ] 발행 절차가 문서화 안 돼 있음 (암호화·커밋 과정) — 회사 컴퓨터에서도 돌릴 수 있게 정리 필요

---

## 결정 대기 (내가 못 정하는 것)

- 관세 0% 가정을 유지한 채 임원 공유를 먼저 할지, confirm 후 공유할지
- 대시보드를 사내 배포할지, 계속 로컬 데모로 쓸지
- 원가모델(`model/`)을 이 레포에 합칠지 분리 유지할지
